option_settings:
  aws:elasticbeanstalk:application:environment:
    PYTHONPATH: "/var/app/current"
    FLASK_DEBUG: "0"
    UPLOAD_FOLDER: "/var/app/current/uploads"
    VERSIONS_FOLDER: "/var/app/current/versions"
    PORT: "8000"
    LANG: "en_US.UTF-8"
    LC_ALL: "en_US.UTF-8"
    DOCUMENT_STORE_PATH: "/var/app/current/document_store"
  aws:elasticbeanstalk:container:python:
    WSGIPath: app:app
    NumProcesses: 1
    NumThreads: 15
  aws:elasticbeanstalk:environment:proxy:
    ProxyServer: nginx
  aws:elasticbeanstalk:application:
    Application Healthcheck URL: "/health"

packages:
  yum:
    python3-pip: []
    python3-devel: []
    gcc: []
    openssl-devel: []
    libffi-devel: []
    bzip2-devel: []

commands:
  01_pip_upgrade:
    command: |
      python3 -m pip install --upgrade pip --user
      python3 -m pip install --upgrade setuptools wheel --user

files:
  "/opt/elasticbeanstalk/hooks/appdeploy/pre/01_set_permissions.sh":
    mode: "000755"
    owner: root
    group: root
    content: |
      #!/usr/bin/env bash
      # Create necessary directories with correct permissions
      mkdir -p /var/app/current/uploads /var/app/current/versions /var/app/current/document_store /var/log/flask
      chown -R webapp:webapp /var/app/current/uploads /var/app/current/versions /var/app/current/document_store /var/log/flask
      chmod -R 755 /var/app/current/uploads /var/app/current/versions /var/app/current/document_store /var/log/flask

  "/etc/nginx/conf.d/proxy.conf":
    mode: "000644"
    owner: root
    group: root
    content: |
      client_max_body_size 100M;
      proxy_connect_timeout 75s;
      proxy_read_timeout 300s;

  "/opt/elasticbeanstalk/hooks/appdeploy/pre/01_set_tmp_permissions.sh":
    mode: "000755"
    owner: root
    group: root
    content: |
      #!/usr/bin/env bash
      chown -R webapp:webapp /tmp
      chmod -R 750 /tmp

container_commands:
  01_upgrade_pip:
    command: "python3 -m pip install --no-cache-dir --upgrade pip setuptools wheel"
  02_install_requirements:
    command: "python3 -m pip install --no-cache-dir -r requirements.txt"
  03_wsgi_permissions:
    command: "chmod 755 app.py"
  04_nginx_reload:
    command: "systemctl reload nginx || systemctl restart nginx"